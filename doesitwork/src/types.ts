export type AdminUser = {
    user_id: string;
    name: string;
    email: string;
};

export type Location = {
    location_id: string;
    parent_id: string;
    name: string;
    icon: string;
};
